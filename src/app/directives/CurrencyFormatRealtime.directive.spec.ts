/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CurrencyFormatRealtimeDirective } from './CurrencyFormatRealtime.directive';

describe('Directive: CurrencyFormatRealtime', () => {
  it('should create an instance', () => {
    const directive = new CurrencyFormatRealtimeDirective();
    expect(directive).toBeTruthy();
  });
});
