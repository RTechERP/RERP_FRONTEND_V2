/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AutocompleteDirectiveDirective } from './autocomplete-directive.directive';

describe('Directive: AutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompleteDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
